[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "customer360"
version = "0.1.0"
description = "Insurance Customer 360 — FastAPI + React on Cloudera AI"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["customer360*"]
